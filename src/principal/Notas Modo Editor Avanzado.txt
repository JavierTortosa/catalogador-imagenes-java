======================================================
NOTAS - MODO EDITOR AVANZADO (Visor de Imágenes V2)
Fecha: 04/08/2026
======================================================

OBJETIVO
--------
Añadir un botón "Modo Editor" en la barra de modos que abra el editor
avanzado a pantalla completa (efecto Photoshop), como un modo de trabajo
dedicado, igual que existen Modo Proyecto, Modo Datos o Modo Render.

DECISIÓN DE ARQUITECTURA
------------------------
NO duplicar el AdvanceEditPanel. El editor ya es un editor de capas
completo y reutilizable. Lo que cambia entre usos es el CONTEXTO/HOST:
  - Modo Render (actual): editor dentro de RenderPanel, alimentado por el
    preview 3D y que "envía a preview".
  - Modo Editor (nuevo): el MISMO AdvanceEditPanel, como modo de trabajo
    propio con persistencia (.edoc JSON + sidecar PNG).
Un solo panel, dos modos de presentación. Se evita duplicar canvas,
controlador de herramientas y lógica de capas.

ENFOQUE: Opción B-lite (WorkMode.EDITOR sin duplicar)
-----------------------------------------------------
Decisiones cerradas con el usuario (04/08/2026):

1. MODO NORMAL, SIN SALIDA ESPECIAL
   - EDITOR es un WorkMode más. No hay "volver al modo anterior": se cambia
     de modo con la barra igual que todos los modos.
   - salirModo/entrarModo guardan/restauran el estado en memoria, como hace
     todo el sistema (semántica de Modo Proyecto).

2. CAMBIOS PENDIENTES = SEMÁNTICA MODO PROYECTO
   - El editor mantiene en memoria lo que se está haciendo.
   - Documento con nombre; antes de guardar es un temporal
     (editor_temporal.edoc).
   - Cualquier modificación en el canvas marca '*' en el título (sin
     guardar). Al guardar desaparece el '*' y el nombre pasa a ser el del
     archivo guardado.
   - Al cerrar la app con '*' pendiente: avisar (Guardar / No Guardar /
     Cancelar). Si "No Guardar": guardar sesión de recuperación.
   - Al reabrir la app y entrar en Modo Editor: avisar de que se dejó un
     documento sin guardar -> cargar o descartar (empezar limpio).

3. FULLSCREEN (dos cosas DISTINTAS)
   - "Fullscreen del modo render" = oculta los paneles laterales de render
     (preview 3D y archivos). ES EL QUE USA EL MODO EDITOR.
     * Al ENTRAR en Modo Editor -> activarlo (editor ocupa toda la zona).
     * Al SALIR -> desactivarlo (se muestran de nuevo preview y archivos).
   - "Fullscreen de la aplicación" = oculta la barra de Windows (ventana
     completa real). NO SE TOCA: lo activa/desactiva el usuario manualmente.
   - ESC NUNCA sale de fullscreen: solo cancela la herramienta activa o
     limpia la selección (comportamiento actual de CanvasController).

4. PERSISTENCIA .edoc (JSON + sidecar PNG)
   - Fichero JSON (Gson) con versionado: cabecera (version, canvas) + capas
     discriminadas por tipo.
   - IMAGE -> referencia src (ruta relativa al .edoc) + transformaciones.
   - SHAPE -> metadatos completos (shapeType, fill/stroke, strokeWidth...).
   - TEXT -> texto + fuente (familia/estilo/talla) + alineación + runs...
   - Capas raster SIN archivo previo (capa nueva, pintada, bote, fusión,
     degradado...) -> se exportan a PNG en carpeta "_capas/" junto al .edoc
     y se referencian por ruta. El JSON queda pequeño y legible.
   - EXTENSIÓN: .edoc (confirmada).

5. RENDER Y EDITOR = DOS DOCUMENTOS INDEPENDIENTES
   - RENDER conserva su propia composición (capas collage/filmstrip + su
     editor interno) en memoria.
   - EDITOR gestiona su propio documento .edoc.
   - Al cambiar de modo: salirModo guarda el documento del modo actual y
     entrarModo carga el del modo destino en el MISMO AdvanceEditPanel
     (swap de CanvasModel+LayerModel, ya soportado por setLayerModel/
     setCanvasModel). Esto respeta la semántica de Modo Proyecto y hace
     útil la copia cruzada.

6. COPIAR / PEGAR IMAGEN
   - Copiar en RENDER = aplanar LO QUE SE VE EN PANTALLA (el preview tal
     cual, con la recolocación y las capas visibles) en UNA imagen.
     * Si está activo el editor avanzado -> EditorComponentBar.composeBufferedImage().
     * Si está la vista 2D normal -> aplanar imageDisplayPanel (collage o currentImage2D).
   - Copiar en VISOR / PROYECTO / CLIENTE -> model.getCurrentImage().
   - Pegar en EDITOR: Ctrl+V + botón CMD_EDITOR_PEGAR_IMAGEN -> crea un
     ImageLayer ("Imagen pegada") centrado en el canvas -> addLayer -> sucio.
   - Portapapeles del sistema (DataFlavor.imageFlavor) -> también pega en
     apps externas (WhatsApp, etc.).

PLAN DE IMPLEMENTACIÓN
----------------------

FASE 1 - Modo Editor como WorkMode  [HECHO 04/08/2026]
1. Enum y comandos  [OK]
   - [OK] VisorModel.java:23 -> añadir EDITOR al enum WorkMode.
   - [OK] VisorModel.getCurrentListContext (239) / getCurrentZoomContext (254):
     añadir case RENDER y case EDITOR junto a VISUALIZADOR (hoy RENDER cae
     al default silencioso; hacerlo explícito).
   - [OK] AppActionCommands.java -> CMD_MODO_EDITOR = "cmd.modo.editor".

2. Barra de modos (UIDefinitionService.java:812-813)  [OK]
   - [OK] Reemplazar el placeholder CMD_FUNCIONALIDAD_PENDIENTE ("Editor Grafico",
     8006-edit-mode.png) por CMD_MODO_EDITOR + tooltip "Modo Editor".
   - [OK] ActionFactory: registerAction(CMD_MODO_EDITOR,
     createSwitchWorkModeAction(WorkMode.EDITOR, ...)) junto a RENDER (690).

3. Cambio de modo (AppModeService)  [OK]
   - [OK] cambiarVistaPrincipal (121) -> case EDITOR -> "VISTA_RENDER"
     (reutiliza la misma tarjeta; sin vista nueva ni reparenting).
   - [OK] sincronizarBotonesModo (250 y 259) -> case EDITOR -> CMD_MODO_EDITOR
     Y añadir CMD_MODO_EDITOR a la lista comandosDeModo (si solo se mete en
     uno, el toggle no se selecciona bien).
   - [OK] entrarModo EDT-1 (426) -> case EDITOR: VISTA_RENDER +
     renderController.activarModoEditor().
   - [OK] salirModo (327) -> case EDITOR: renderController.desactivarModoEditor()
     + guardado del documento temporal si sucio (Fase 2) [esto último pendiente Fase 2].
   - [OK] reparentSharedDisplayContainer: EDITOR cae en default -> null -> sin
     reparenting (seguro).

4. Nuevos métodos en RenderController  [OK]
   - [OK] activarModoEditor(): reutiliza la inicialización idempotente de
     toggleAdvanceEditMode() (CanvasModel/LayerModel/CanvasController si es
     la 1ª vez) + panel.setAdvanceEditActive(true) + panel.setEditorFullscreen(true).
   - [OK] desactivarModoEditor(): setAdvanceEditActive(false) + setEditorFullscreen(false).

5. RenderPanel  [OK]
   - [OK] Nuevo setEditorFullscreen(boolean) IDEMPOTENTE (no toggle) para
     ocultar/mostrar los paneles laterales (preview + archivos).

6. ESC  [HECHO]
   - [OK] AdvanceEditPanel.setCanvasController (530-538): cambiar
     setFullscreenEscapeHandler para que devuelva false SIEMPRE (que ESC
     nunca salga de fullscreen).
   - [HECHO] Ocultar el botón interno de fullscreen del editor cuando se está en
     Modo Editor (el fullscreen ya está activado): AdvanceEditPanel.setModoEditorActivo
     (campo fullscreenToggleButton, oculto en buildLeftToolbar).

7. Toolbars  [HECHO]
   - [OK] ToolbarManager.reconstruirContenedorDeToolbars (143): se oculta
     barra_estado_controles (229) en EDITOR y se añadió EDITOR a modo/modo_bottom.
   - [HECHO] barra_editor (def con modosVisibles=[EDITOR]) -> Fase 2 (item 7).

FASE 2 - Documento .edoc (semántica Modo Proyecto)
1. Nuevo servicios/editor/EditorDocumentManager (espejo de ProjectManager):
   - Estado: dirty ('*'), nombreDocumento, archivoActivo (null = temporal),
     archivoTemporal = editor_temporal.edoc, referencia a CanvasModel+LayerModel.
   - API: hayCambiosSinGuardar(), markDocumentAsSaved(), notificarModificacion(),
     nuevoDocumento(), abrirDocumento(Path), guardarAArchivo(), guardarComo(Path),
     guardarSesionDeRecuperacion(), eliminarSesionDeRecuperacion().
   - Se marca sucio desde LayerModel.changeListener + operaciones del
     CanvasController (pintar, transformar).   [HECHO: LayerModel + Tool.modifiesContent()/CanvasController.setContentChangeCallback]
   [HECHO] Clase EditorDocumentManager.java (servicios/editor/), EditorDoc.java (DTO),
     ConfigKeys EDITOR_*, module-info opens servicios.editor+modelo.editor a gson,
     ImageLayer.srcPath, LayerModel multi-listeners.
   [HECHO] guardarSesionDeRecuperacion/eliminarSesionDeRecuperacion/marcarComoRecuperado
     (recovery autocontenido en carpetaBaseDocs, sin alterar el doc activo).
   [HECHO] marcar sucio al pintar/transformar (Tool.modifiesContent() en EditTool,
     TransformTool, PaintBucketTool, GradientTool, ShapeTool, TextTool, CropTool;
     CanvasController.notifyContentChanged() en mouseReleased).
2. Serialización JSON (Gson) versionada:
   - Cabecera: version, canvas (w/h/bg/transparent).
   - Capas por tipo: IMAGE (src + bounds/rotación/opacidad/visible/bloqueada),
     SHAPE (shapeType, fill/stroke ARGB, strokeWidth, renderW/H),
     TEXT (texto, fuente familia/estilo/talla, color, alineación, vertical,
     subrayado, tachado, lineSpacing, autoSize, flowColumns, runs).
   - Adapters Gson para Rectangle, Color, Font, TextRun.  [OK: se usan DTO con primitivas/string]
   - ImageLayer gana campo srcPath; en guardado, capas sin origen -> PNG en
     "_capas/<id>.png" junto al .edoc.  [HECHO]
   - module-info.java: opens modelo.editor to com.google.gson.  [HECHO]
3. Título con '*': ViewManager.actualizarTituloVentana (1402) añade, cuando
   WorkMode == EDITOR, "[Documento: <nombre>]" con prefijo '*' si sucio
   (mismo patrón que proyecto).  [HECHO: ViewManager.setEditorDocumentoTitulo + sufijo; RenderController.refrescarTituloEditor lo empuja; se limpia al salir]
4. Salida del modo: AppModeService.salirModo(EDITOR) guarda a
   editor_temporal.edoc si hay sucio y no hay archivo con nombre (espejo de
   VISUALIZADOR en AppModeService:349-366).  [HECHO: en AppModeService.salirModo EDITOR]
5. Cierre de app: integrar en ProjectLifecycleService.handleApplicationShutdown
   (187) - si el editor tiene '*' -> diálogo Guardar/No Guardar/Cancelar; si
   "No Guardar" -> guardarSesionDeRecuperacion() + clave de config
   editor.recuperacion.pendiente.  [HECHO: GeneralController.gestionarDocumentoEditorAlCerrarApp + ConfigKeys.EDITOR_RECUPERACION_PENDIENTE]
6. Recuperación al entrar: GeneralController.gestionarRecuperacionAlEntrarModo
   (527) o método paralelo para EDITOR -> "Dejaste un documento sin guardar.
   ¿Cargarlo o descartar?"; cargar -> abrirDocumento, descartar -> nuevoDocumento.
   [HECHO: GeneralController.gestionarRecuperacionAlEntrarModoEditor + pre-validación en cambiarModoDeTrabajo; RenderController.inicializarCanvasEditor/getOrCreateEditorDocumentManager]
7. Toolbar del editor: ToolbarDefinition "barra_editor" (modosVisibles=[EDITOR],
   LEFT) con botones Nuevo/Abrir/Guardar/Guardar como (CMD_EDITOR_NUEVO/
   ABRIR/GUARDAR/GUARDAR_COMO), acciones delegando al RenderController/
   EditorDocumentManager.  [HECHO: barra_editor + 4 acciones + diálogos en RenderController + botón Pegar]

FASE 2.5 - Copiar/Pegar imagen
1. Nuevo utils/ImageClipboard: copy(BufferedImage) y read() con
   DataFlavor.imageFlavor (patrón de CropDialog:130 y ViewManager:1108).
   [HECHO: controlador/utils/ImageClipboard.java]
2. Comando global CMD_COPIAR_IMAGEN (sensible al contexto, habilitado si hay
   imagen actual):
   - VISUALIZADOR / PROYECTO / CLIENTE -> model.getCurrentImage().
   - RENDER -> aplanar lo visible (editor activo: composeBufferedImage;
     vista 2D: imageDisplayPanel).
   - Exponer en el menú contextual de la imagen (junto a "Copiar nombre" de
     MenuPopupManager:235) y opcionalmente en toolbar.
   [HECHO: acción registrada (ActionFactory.createCopiarImagenAction -> copiarImagenActual),
    RenderController.copiarImagenVisible + EditorComponentBar.flattenVisibleEditorImage,
    expuesto en el menú contextual de la imagen (MenuPopupManager, junto a "Copiar nombre")]
3. Pegar en el editor: Ctrl+V en AdvanceEditPanel/CanvasPanel (hoy no hay
   pegado) + botón CMD_EDITOR_PEGAR_IMAGEN en el home/toolbar -> ImageLayer
   centrado -> addLayer -> marcar sucio.
   [HECHO: CMD_EDITOR_PEGAR_IMAGEN en barra_editor + Ctrl+V (CanvasController KeyHandler)
    + RenderController.pegarImagenEditor (ImageLayer centrado + sucio)]

FASE 2.6 - Documentos independientes RENDER/EDITOR  [HECHO]
- Swap del documento en el mismo AdvanceEditPanel al cambiar de modo:
  salirModo guarda el documento del modo actual, entrarModo carga el del
  modo destino (AdvanceEditPanel.setLayerModel:430 / setCanvasModel:447).
- RENDER conserva su composición en memoria; EDITOR su .edoc.
- La copia cruzada se hace por portapapeles (Fase 2.5).
- Implementación: RenderController mantiene dos slots (canvasModelRender+
  layerModelRender y canvasModelEditor+layerModelEditor). El panel muestra
  uno u otro según el modo activo vía cargarDocumentoEnPanel(cm,lm,esEditor),
  que además hace canvasController.setContext(...) y re-engancha
  EditorDocumentManager.setDocument(cm,lm) cuando el doc es el del EDITOR
  (setLayerModel reinstala el listener de la vista y limpia los demás).
- LayerModel.removeChangeListener(Runnable) añadido; EditorDocumentManager
  guarda capasChangeListener (Runnable fijo) y setDocument es idempotente.
- toggleAdvanceEditMode es no-op en Modo Editor (no desactiva el editor).
- notificarModificacionDocumentoActivo() solo ensucia si el doc EDITOR está
  cargado en el panel; pegarImagenEditor usa el documento activo del panel.

FASES FUTURAS (no incluidas en Fase 1/2)
----------------------------------------
- Fase 3: Efectos/ajustes de imagen (brillo, contraste, filtros, texturas).
  NOTA: el home del editor YA tiene presets de lienzo, nuevo/medidas/ajustar
  al contenido, importar imagen, exportar imagen y enviar a preview.
  Lo que falta son los efectos de imagen (los editores de efectos no están
  implementados).
- Historial / undo / redo.
- Exportar aplanado a PNG desde Modo Editor.

DATOS CLAVE DEL CÓDIGO (referencia)
-----------------------------------
- WorkMode enum: src/modelo/VisorModel.java:23-30
- Editor avanzado es tarjeta CARD_GRID_ADVANCE_EDIT en RenderPanel
  (src/vista/panels/render/RenderPanel.java:74,317)
- Activación actual: CMD_PREVIEW_RENDER_ADVANCE_EDIT ->
  RenderController.toggleAdvanceEditMode() (src/controlador/RenderController.java:1735)
- Swap de documento: AdvanceEditPanel.setLayerModel (430) / setCanvasModel (447)
- Aplanar canvas del editor: EditorComponentBar.composeBufferedImage() (2579)
- Vista 2D render (collage/capas): RenderPanel.imageDisplayPanel (332-371)
- Imagen 2D actual del render: RenderPanel.getCurrentImage2D() (1060)
- Fullscreen editor (toggle): RenderPanel.toggleEditorFullscreen() (1020)
- ESC editor: AdvanceEditPanel.installEscapeBinding (265) ->
  CanvasController.handleEscape (209) -> fullscreenEscapeHandler (530-538)
- Barra de modos: UIDefinitionService.java:799-814 (placeholder editor: 812)
- Sincronización toggle: AppModeService.sincronizarBotonesModo (243-276)
- Vista render: ViewBuilder.java:386-393 (VISTA_RENDER)
- Gestión de modos: AppModeService.cambiarVistaPrincipal (121),
  actualizarUiModo (143), salirModo (327), entrarModo (426)
- Acciones de modo: ActionFactory.createSwitchWorkModeAction (859)
- Título con '*' de proyecto: ViewManager.actualizarTituloVentana (1402)
- Patrón sucio/temporal/recuperación: ProjectManager (hayCambiosSinGuardar,
  markProjectAsSaved, guardarAArchivo, archivoSeleccionTemporalPath:117,
  guardarSesionDeRecuperacion/eliminarSesionDeRecuperacion:1234)
- Cierre de app: ProjectLifecycleService.handleApplicationShutdown (187) /
  promptToSaveChangesIfNecessary (153)
- Recuperación al entrar en modo: GeneralController.gestionarRecuperacionAlEntrarModo (527)
- Salvar temporal del visualizador: AppModeService.salirModo VISUALIZADOR (349-366)
- Portapapeles texto: ViewManager.copiarListaAlPortapapeles (1108)
- Portapapeles imagen (ejemplo): CropDialog (130-136)
- Imagen actual en modelo: VisorModel.getCurrentImage() (campo currentImage:58)
- Capa de imagen (sin srcPath hoy): modelo/editor/ImageLayer.java

PLAN - Botones del panel Herramientas (Alinear/Distribuir/Espacio/Auto distribuir)
----------------------------------------------------------------------------------
SEMANTICA DECIDIDA (usuario):
- Alinear: respecto al bounding box de la seleccion (union de las capas
  seleccionadas); con una sola capa activa, respecto al lienzo.
- Distribuir: reparte el espacio entre los bordes del bounding box de la
  seleccion (primera y ultima capa en los extremos del rango).
- Espacio: hueco uniforme entre capas adyacentes, sin mover la primera ni la
  ultima (estilo Photoshop "Distribute Spacing").
- Unificar: los combos de Alinear/Distribuir de la barra superior
  (EditorComponentBar) reutilizan la misma logica que los botones del panel.

ESTADO ACTUAL:
- AdvanceEditPanel.createButtonFromDef (941-979): solo AUTO_DISTRIBUTE (3)
  tienen manejador; ALIGN (6), DISTRIBUTE (6) y ESPACIO (2) caen al stub.
- EditorComponentBar.executeAlign (479) alinea solo la capa activa contra el
  canvas; executeDistribute/distributeEvenly (506-567) son crudos. Espacio no
  existe en ningun sitio.
- AutoDistributeActions implementado (fija/porCapa/escalarParaAjustar).

IMPLEMENTACION [HECHO]:
1. Nueva clase controlador/actions/editoravanzado/LayerDistributionActions.java
   con alinear(lm, cm, hMode, vMode), distribuir(lm, cm, mode) y
   espaciar(lm, cm, horizontal). Capas objetivo = seleccionadas visibles y no
   bloqueadas; si seleccion < 2 se usan todas las visibles/no bloqueadas.
   - alinear: ref = union de capas objetivo si seleccion multiple, si no el
     lienzo. x = ref.x + (ref.w - b.w)*k; y = ref.y + (ref.h - b.h)*k.
   - distribuir: ordena por borde/centro, fija rango entre primer/ultimo punto
     dentro del bounding box de la seleccion y reparte a intervalos iguales.
   - espaciar: primera/ultima fijas; gap = (span - sumaTamanyos)/(n-1).
   Tras cada operacion: canvas.repaint().
2. AdvanceEditPanel.createButtonFromDef -> createActionFromCommand: dispatch
   para los 14 comandos de ALIGN/DISTRIBUTE/ESPACIO (antes stub). Usa
   editorLayerModel ya existente. Ayudante simpleAction(Runnable).
3. EditorComponentBar: executeAlignFromCommand -> alinear;
   executeDistributeFromCommand -> distribuir; eliminados los bodies crudos
   executeAlign/executeDistribute/distributeEvenly. createIconComboButton OK.
   Imports sin uso eliminados (ArrayList, List, CanvasModel, LayerModel).
4.    JavaDoc + cierre de metodos segun reglas. Compila (mvn compile EXIT=0).
   HTMLs: pendiente cuando lo pida el usuario.

SELECCION MULTIPLE EN EL CANVAS [HECHO]
- EditTool.mousePressed: seleccion estilo Photoshop directamente sobre el
  lienzo (misma semantica que LayerCard):
  - clic normal  -> setSingleSelection
  - Ctrl+clic    -> toggleSelected (anade/quita de la seleccion)
  - Shift+clic   -> selectRange (rango desde la capa activa)
  - clic en vacio -> deselecciona (si auto-seleccion activa)
  Se elimino el comportamiento anterior de Ctrl+clic "activar temporal y
  restaurar al soltar" (campo ctrlRestoreLayer y sus usos).
- LayerPicker: beginMove(p, List<Layer>) mueve varias capas juntas; DragMove
  guarda los bounds de partida por capa (mapa por getId) para cancelar.
  beginMove(p, Layer) delega en la version multiple.
- EditTool.mousePressed paso 5: el arrastre mueve movableSelectedLayers()
  (seleccionadas visibles y no bloqueadas; si no hay, la activa).
- EditTool.keyPressed (flechas): mueve todas las seleccionadas.
- Compila (mvn compile EXIT=0).

FIX #2 - Alinear centros segun iconos [HECHO]
- Los iconos indican la semantica real: 70402-align-centro-vertical.png =
  centros en linea VERTICAL (mismo centerX) -> ALIGN_HCENTER;
  70405-align-centro-horizontal.png = centros en linea HORIZONTAL (mismo
  centerY) -> ALIGN_VCENTER.
- AdvanceEditPanel.createActionFromCommand y
  EditorComponentBar.executeAlignFromCommand corregidos: CMD_ALIGN_CENTRO_VERTICAL
  -> ALIGN_HCENTER, CMD_ALIGN_CENTRO_HORIZONTAL -> ALIGN_VCENTER (estaban
  intercambiados). Compila (mvn compile EXIT=0).

FIX #1 - Marco global (union) del gizmo con seleccion multiple [HECHO]
- Al marcar una segunda capa, el gizmo ya no se queda solo en la capa activa:
  ahora dibuja y opera sobre la UNION de los bounds de las capas objetivo
  (marco global estilo Photoshop) cuando hay seleccion multiple.
- EditTool: helpers gizmoTargets() (= movableSelectedLayers()) y
  gizmoBounds(List<Layer>) (union si >1, si no los bounds de la unica capa).
  Se usan en mousePressed (hit-test), paintOverlay (dibujo), getCursor y
  paso 5 (arrastre: mover dentro del marco mueve TODAS las seleccionadas).
  La rotacion del marco se fuerza a 0 en modo multiple (la union es axis-aligned).
- LayerPicker: unionBounds(List<Layer>); beginMultiGizmo -> MultiGizmoDrag
  (recoloca/re-escala cada capa con coordenadas relativas al marco inicial;
  con escala 1:1 equivale a mover); beginMultiRotate -> MultiRotateDrag (cada
  capa rota su centro alrededor del centro de la union y suma el incremento de
  angulo a su propia rotacion). Se conservan beginGizmo/beginRotate/GizmoDrag/
  RotateDrag de capa unica (los usa TransformTool). EditTool ya no hace
  active.setBounds(newBounds) tras el drag del gizmo: MultiGizmoDrag aplica
  internamente.
- EditTool.mousePressed paso 3: clic sobre una capa YA seleccionada mantiene
  el conjunto (solo setActiveLayer) en vez de colapsar a seleccion simple,
  permitiendo arrastrar el marco con todas las capas.
- Compila (mvn compile EXIT=0).

FIX DISTRIBUTIR - dispersion infinita [HECHO]
- Bug: distribuir calculaba el rango con computeReference (union de los BORDES
  de las capas) pero colocaba los CENTROS en esos extremos. Como la referencia
  se recalcula en cada pulsacion y los centros quedaban mas alla de los bordes,
  la union crecia en cada pulsacion y las capas se separaban hasta el infinito.
- Fix: el rango ahora se calcula sobre la propia magnitud repartida (borde,
  centro o borde opuesto segun el modo), tomando los valores extremos de la
  primera y ultima capa. Asi la operacion es idempotente: primera/ultima se
  quedan fijas y las intermedias se reparten a intervalos iguales entre ellas
  (estilo Photoshop). Con 2 capas no cambia nada (ya estan en los extremos).
- Se elimino el parametro canvasModel (sin uso) de distribuir y espaciar;
  actualizados los llamadores (AdvanceEditPanel.createActionFromCommand y
  EditorComponentBar.executeDistributeFromCommand). computeReference queda solo
  para alinear. Compila (mvn compile EXIT=0).

FIX #3 - Distribuir centros segun iconos [HECHO]
- Mismo criterio que el FIX #2 de Alinear: los iconos indican la semantica real,
  asi que CMD_DISTRIBUTE_CENTER_VERTICAL -> DIST_CENTER_HORIZONTAL y
  CMD_DISTRIBUTE_CENTER_HORIZONTAL -> DIST_CENTER_VERTICAL.
- Aplicado en AdvanceEditPanel.createActionFromCommand y en
  EditorComponentBar.executeDistributeFromCommand (estaban intercambiados).
- Tooltips corregidos en UIDefinitionService.getComponentesLayerDistribute:
  70501-bottom-border -> "Distribuir Bordes Inferiores",
  70503-top-border -> "Distribuir Bordes Superiores" (estaban al reves).
- Compila (mvn compile EXIT=0).

FIX #4 - Deshabilitar botones Distribuir con <3 capas [HECHO]
- Con menos de 3 capas efectivas Distribuir no tiene efecto (primera/ultima
  fijas), asi que los botones del grupo Distribuir del panel Herramientas y el
  combo Distribuir de la barra superior se deshabilitan.
- LayerDistributionActions.countDistributionTargets(layerModel): mismo criterio
  que getDistributionTargets (seleccion si >=2, si no todas las visibles/no
  bloqueadas) pero devuelve solo el conteo.
- AdvanceEditPanel: lista distributeButtons recogida por createSectionFromDefs
  (sobrecarga con colector); updateDistributeButtonsState() se invoca en
  setLayerModel, en el changeListener del LayerModel y tras buildDefaultTools.
- EditorComponentBar: campo distributeComboButton + updateDistributeButtonState()
  (consulta el AdvanceEditPanel via findAdvanceEditPanel).
- Compila (mvn compile EXIT=0).

PANEO DEL CANVAS [HECHO]
- Bug: el canvas del editor solo tenia zoom con la rueda del raton; no habia
  ningun gesto de paneo (offsetX/offsetY existian pero nada los modificaba).
- Fix (1a parte): arrastre con el BOTON CENTRAL del raton paneea el lienzo
  (CanvasController.MouseHandler): mousePressed BUTTON2 inicia el paneo y no
  delega a la herramienta, mouseDragged desplaza el offset del CanvasPanel y
  mouseReleased lo termina. El cursor cambia a MOVE durante el paneo
  (updateCursor) y el estado se limpia al cambiar de herramienta
  (setActiveTool).
- Fix (2a parte, "el canvas como fondo"): el clic IZQUIERDO sobre la zona
  oscura FUERA del rectangulo visible del lienzo tambien inicia el paneo
  (isOutsideCanvas: punto fuera de [offsetX, offsetY] + canvasW*zoom x
  canvasH*zoom). Asi, pulsar sobre una capa mueve la capa (dentro del lienzo)
  y pulsar sobre el fondo mueve el canvas. El boton central sigue panendo en
  cualquier sitio (util si el lienzo cubre todo el panel).
- Fix (3a parte, capas que sobresalen del lienzo): una capa puede estar mitad
  dentro y mitad fuera del canvas. Ahora el clic izquierdo fuera del lienzo NO
  panea si hay una capa bajo el cursor (layerPicker.findLayerAt en coordenadas
  del canvas): el clic llega a la herramienta y selecciona/arrastra la capa
  igual que dentro del lienzo. Si el clic fuera del lienzo cae sobre zona
  vacia (sin capa), panea Y deselecciona todas las capas (clearAllSelection +
  selectionModel.clear), que es lo que espera el usuario al pulsar fuera.
- No se uso Espacio+arrastre porque el GlobalInputManager (KeyEventDispatcher
  global) consume VK_SPACE para CMD_PROYECTO_TOGGLE_MARCA. Compila EXIT=0.

PANEL "DISPOSICION" + REFERENCIA DE ALINEACION [HECHO]
- Panel único "Disposicion" (acordeon EXCLUSIVO) en el panel Herramientas:
  las secciones Alinear, Distribuir, Espacio, Auto distribuir y Opciones se
  apilan una encima de la otra; al expandir una se colapsan las demas (estilo
  acordeon). Estado inicial: "Alinear" expandido, el resto colapsado.
- AdvanceEditPanel: createSectionInternal(...) extrae la logica de colapso de
  createSection; createSectionExclusive(title, content, grupoDisposicion)
  registra la accion de colapso propia en el grupo y al expandir ejecuta el
  colapso de las demas. createGridFromDefs(defs, columns, collectInto) devuelve
  la cuadricula sin cabecera (contenido de cada seccion). createSection y
  createSectionFromDefs conservan su comportamiento (independiente).
- Referencia de alineacion configurable (solo Alinear, en memoria, compartida
  por panel y combos de la barra): LayerDistributionActions.ReferenceMode enum
  { LIENZO, SELECCION, CAPA_MAESTRA } + estado estatico get/setReferenceMode
  (default SELECCION) y get/setMasterLayerId. computeReference(targets, lm, cm,
  mode): LIENZO -> lienzo; SELECCION -> union de las capas objetivo (con <2 cae
  al lienzo para no romper el comportamiento de capa unica); CAPA_MAESTRA ->
  bounds de la capa maestra (visible/no bloqueada, fallback a SELECCION).
- Seccion "Opciones": groupbox "Referencia de alineacion" con 3 JRadioButton
  (Lienzo/Seleccion/Capa maestra) + controles de capa maestra (boton "Usar capa
  activa como maestra", etiqueta con el nombre, boton "Quitar"). Al cambiar el
  modo o la maestra se actualiza LayerDistributionActions y la etiqueta
  (refreshDisposicionMasterControls, conectada tambien al changeListener del
  LayerModel para reflejar renombrados/borrados).
- Alinear con seleccion multiple ya usaba la union de bounds (capa virtual
  Photoshop): 100,100 + 200,200 -> centro 150. Confirmado. Distribuir y
  Espaciar conservan su semantica de extremos (primera/ultima fijas);
  AutoDistributeActions sigue usando el lienzo. Compila (mvn compile EXIT=0).

HISTORIAL UNDO/REDO [HECHO]
- Todo original en src/principal/VisorV2.java:52 (bloque EDITOR AVANZADO).
- Alcance: SOLO el documento del Modo Editor (slot EDITOR). El slot RENDER
  queda fuera: los callbacks de gesto/undo/redo llevan el guard
  editorDocumentoCargadoEnPanel.
- Arquitectura: servicios/editor/EditorHistory.java -> historial con snapshots
  inmutables del documento (lienzo w/h/fondo/transparente + capas clonadas en
  profundidad + seleccion + capa activa). MAX_PASOS=50. Se suscribe a los
  cambios estructurales del LayerModel (auto-commit por comparacion
  mismoContenido, que IGNORA la seleccion) y recibe onNotified() desde
  EditorDocumentManager.notificarModificacion().
- Clonado profundo SIEMPRE (decision): Layer.copiaParaHistorial() +
  Layer.mismoEstado(Layer) anadidos a la interfaz; ImageLayer (clona el
  BufferedImage via Graphics2D.drawImage, conserva id/nombre/srcPath) y
  TextLayer (copia fields rich text). ImageLayer.copy()/TextLayer.copy() NO se
  usan (generan UUID nuevo y sufijo " (copia)").
- Gestos: CanvasController.mousePressed dispara gestureStartCallback (si la
  herramienta modifica contenido) -> RenderController.iniciarGestoEditor ->
  beginGesture(nombreHerramienta, esPixeles). La transaccion agrupa las
  mutaciones del arrastre; al soltar, notifyContentChanged ->
  notificarModificacion -> onNotified cierra la transaccion y empuja el paso.
  El bote de pintura (PaintBucketTool) marca esPixeles=true: muta pixeles
  in-place y no es detectable por comparacion, asi que el paso se fuerza.
- Operaciones discretas con nombre: record(nombre, op) en
  AdvanceEditPanel.ejecutarConHistorial (Alinear/Distribuir/Espacio/Auto
  distribuir), EditorComponentBar.applyTextProperty/applyShapeProperty
  (Propiedades de texto/forma), crearNuevoLienzo/aplicarMedidasLienzo/
  ajustarLienzoAlContenido (Nuevo lienzo/Redimensionar lienzo/Ajustar lienzo),
  RenderController.pegarImagenEditor (Pegar imagen). El slider de opacidad usa
  beginGesture/endGesture con MouseListener -> 1 paso por arrastre ("Opacidad").
- EditorDocumentManager.setDocument crea o rebindea el historial (rebind
  resuscribe el listener del LayerModel y hace clear). nuevoDocumento y
  abrirDocumento hacen clear tras aplicar. notificarModificacion delega primero
  en history.onNotified() y despues en la logica de dirty.
- Capas del documento actual: restaurarEstado(List<Layer>, Set<Integer>, int)
  en LayerModel (un solo fireChanged). Flag aplicando en EditorHistory evita
  re-entradas (el fireChanged de restaurarEstado dispararia autoCommit).
- UI: EditorComponentBar.buildRow1RightSide ahora prepone 3 botones antes de
  los checkboxes/combos: Deshacer (82101-undo.png, Ctrl+Z), Rehacer
  (82102-redo.png, Ctrl+Y / Ctrl+Shift+Z) e Historial (82100-historial.png,
  abre JPopupMenu con listaPasos, paso actual con ">" + negrita, clic salta).
  setEditorHistory conecta el historial y updateHistoryButtons refresca
  enabled. El cambio de estado del historial llega por setCambioCallback
  conectado desde RenderController.conectarHistorialConBarra.
- Atajos: CanvasController.KeyHandler maneja Ctrl+Z (undo), Ctrl+Y y
  Ctrl+Shift+Z (redo) cuando el documento del editor esta cargado.
- Icons aportados por el usuario en resources/iconos/{black,white}/:
  82100-historial.png, 82101-undo.png, 82102-redo.png.
- LayerModel solo notifica cambios estructurales (los setBounds/propiedades
  son silenciosos); por eso las propiedades de texto/forma/opacidad van
  envueltas en record/gesto.
- Pendiente si se quiere pulir: cambios de visibilidad/nombre/bloqueo desde
  LayerCardPanel son silenciosos y viajan en el siguiente paso estructural
  (autoCommit). Compila (mvn compile EXIT=0).

ATAJO BORRAR CAPA (Supr / Retroceso) [HECHO]
- Supr o Retroceso en el canvas (con el documento del EDITOR cargado) elimina
  la capa activa o, si hay seleccion multiple, todas las capas seleccionadas.
  La operacion se registra en el historial como paso "Eliminar capa" (undo/redo
  cubre el borrado).
- CanvasController: nuevo callback deleteCallback + setDeleteCallback; el
  KeyHandler lo dispara con clean && (VK_DELETE || VK_BACK_SPACE) y consume el
  evento (se excluye con Ctrl/Alt/Meta para no interferir con otros atajos).
- RenderController: setDeleteCallback(this::borrarCapaEditor) en la
  inicializacion del canvas; borrarCapaEditor() guarda editorDocumentoCargadoEn
  Panel, obtiene los indices de la seleccion (o la activa si no hay), los
  ordena descendente y ejecuta lm.removeLayer(idx) dentro de
  history.record("Eliminar capa", ...). Despues refresca barra y canvas.
- Borrar en orden descendente de indices originales: correcto porque
  removeLayer + adjustIndicesAfterRemove mantienen validos los menores.
- Ayuda: editor_avanzado.html tabla de atajos + fila "Borrar capa" del panel de
  capas. Compila (mvn compile EXIT=0).

SISTEMA AUTO LAYOUT [HECHO]
- Sustituye al antiguo "Auto distribuir" (AutoDistributeActions eliminado).
  13 herramientas que actuan SOLO sobre capas seleccionadas+visibles+no
  bloqueadas (sin seleccion -> no-op). Las no seleccionadas se sacan del lienzo
  o se ignoran segun config.
- Arquitectura (nuevo paquete
  controlador/actions/editoravanzado/autolayout): Configuracion -> Motor ->
  Algoritmos.
  * AutoLayoutAlgorithm (interface) y AutoLayoutContext (CanvasPanel + LayerModel
    + CanvasModel + AutoLayoutConfig via ctx.getConfig()). Los algoritmos solo
    usan el contexto: nada de singletons, UI ni undo/repaint.
  * AutoLayoutConfig (singleton): margin=24, spacing=20, fitMargin=24,
    heroScale=0.5, mosaicVariance=0.15, packMode=SHELF,
    noSeleccionadas=SACAR_FUERA. Enums PackMode{SHELF,SKYLINE,GUILLOTINE} y
    NoSeleccionadasMode{SACAR_FUERA,IGNORAR}.
  * AutoLayoutEngine.aplicar(canvas, lm, algoritmo): construye ctx, layout(),
    canvas.repaint(). El historial lo envuelve AdvanceEditPanel (1 paso undo).
  * AutoLayoutSupport: getTargets(ctx), apartarNoSeleccionadas(ctx), bboxDe(ctx).
- Implementaciones: grid/{GridUniform, GridKeepSize, Rows, Columns, Mosaic},
  pack/PackLayout (SHELF por altura desc, SKYLINE por area desc sobre perfil,
  GUILLOTINE dividiendo rectangulos libres), HeroLayout (hero = activa si esta
  en seleccion, si no la de mayor area), SpiralLayout (espiral de Arquimedes),
  fit/{FitCanvas, CenterHorizontal, CenterVertical}, equal/{EqualHeight,
  EqualWidth} (referencia = activa o primera seleccionada).
- Comandos nuevos en AppActionCommands: CMD_PREVIEW_RENDER_AUTO_LAYOUT_* (13),
  con GRID_UNIFORM (no GRID). Eliminados los 3 AUTO_DISTRIBUTE_*.
- UIDefinitionService.getComponentesLayerAutoDistribute: actualizado a los
  comandos nuevos; CENTER_VERTICAL usa icono 70711 (antes 70710 duplicado).
- AdvanceEditPanel: createActionFromCommand mapea los 13 comandos via
  autoLayoutAction(nombre, algoritmo) -> ejecutarConHistorial. Panel "Auto
  Layout" con 3 filas (buildAutoLayoutPanel + buildAutoLayoutRow/Group +
  createAutoLayoutButton): F1 [Grid, Grid Keep]|[Hero, Espiral, Mosaico],
  F2 [Compactar, Filas, Columnas, Ajustar al Canvas],
  F3 [Centrar H/V]|[Igualar Altura, Anchura].
- Opciones: sub-bloque "Auto Layout" en buildDisposicionOpciones con radios de
  capas no seleccionadas (fuera/ignorar), spinners Espaciado/Margen/Margen
  ajustar, sliders Escala hero/Var. mosaico y combo Empaquetado, todos
  conectados a AutoLayoutConfig.
- Reglas generales: no toca rotacion/opacidad/orden Z/nombre/seleccion; una
  operacion undo; determinista; mantiene proporcion; sin coordenadas negativas
  salvo sacar fuera del lienzo. Plan congelado en "Notas Sistema Auto Layout.txt".
- Ayuda: editor_avanzado.html nueva seccion "Opciones — Sistema Auto Layout".
  Compila (mvn compile EXIT=0).

SMART GUIDES + CONFIG AVANZADA EDITOR [HECHO]
- Liberada la pestana "Opciones" del panel Disposicion: eliminado el sub-bloque
  "Auto Layout" de buildDisposicionOpciones. Quedan "Referencia de alineacion"
  (accion en tiempo real) y los controles de capa maestra.
- Los parametros persistentes del editor se trasladan a Configuracion Avanzada
  con nueva seccion "Editor" (vista/configuracion/panels/EditorPanel.java,
  implementa ConfigurationPanel; nodo de arbol + registerPanel en
  ConfigurationDialog). Contiene dos subsecciones:
  * Smart Guides: activar, ajustar a lienzo, ajustar a capas, distancia de
    ajuste (6), distancia de retencion (3), color (#FF00FF) via JColorChooser y
    grosor de linea (1.0).
  * Auto Layout: no seleccionadas (fuera/ignorar), espaciado (20), margen (24),
    margen ajustar (24), escala hero (50%), var. mosaico (15%) y empaquetado.
- Nuevas claves en ConfigKeys (seccion EDITOR): EDITOR_AUTOLAYOUT_* (7) y
  EDITOR_SMART_GUIDES_* (7). EditorPanel.save() persiste solo lo cambiado y
  sincroniza los singletons AutoLayoutConfig/SmartGuidesConfig;
  ConfigApplicationManager.sincronizarConfigEditor() los recarga desde claves
  en arranque/restaurar.
- Smart Guides (nuevo paquete modelo/editor/smartguides):
  * SmartGuidesEngine (motor puro, sesion por arrastre): begin(canvasRect,
    staticBounds, movingStartBounds) cachea los bounds; update(dx,dy,cfg)
    calcula el snap sobre la union movil contra bordes/centros de capas
    estaticas (snapToLayers) y lienzo (snapToCanvas), con histuresis: guia
    retenida se mantiene mientras |diff| <= stickyDistance. Devuelve
    SnapResult(dx,dy,guides) con GuideLine(vertical,pos,start,end).
  * Sin singletons ni UI: la config se inyecta en update().
  * EditTool: beginSmartGuides() en mousePressed (branch mover y Alt+duplicar),
    updateSmartGuides() en mouseDragged aplica dx/dy corregidos a move.move();
    mantener CTRL durante el arrastre anula snap+guias (smartGuidesOverride);
    endSmartGuides() en mouseReleased/cancel. paintOverlay dibuja las guias a
    linea completa del lienzo con color/grosor configurados.
- Ayuda: editor_avanzado.html seccion "Smart Guides" + referencias a
  Configuracion Avanzada -> Editor; modo_editor.html apartado Smart Guides.
  Compila (mvn compile EXIT=0).

